[project]
name = "mixerlint"
version = "0.1.0"
description = "Lint checker for Behringer XR18 / X32 mixers via OSC"
requires-python = ">=3.11"
dependencies = [
    "rich>=13.0",
    "pyyaml>=6.0",
]

[project.scripts]
mixerlint = "mixerlint:main"
